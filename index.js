import 'expo-dev-client';  // Ensure the dev client is loaded
import { registerRootComponent } from 'expo';  // Import Expo's root component registration
import App from './App';  // Import your main App component

registerRootComponent(App);  // Register your root component with Expo
